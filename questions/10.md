10. Now switch the inputs: ./x86.py -p wait-for-me.s -a
ax=0,ax=1 -R ax -M 2000 How do the threads behave? What
is thread 0 doing? How would changing the interrupt interval (e.g.,
-i 1000, or perhaps to use random intervals) change the trace out-
come? Is the program efficiently using the CPU?


* the thread that has a register that equals 0 will go through a forever loop because it loops through waiter without halting

* the longer it takes to interrupt the thread, the more loops the thread with the 0 register will go through, the less interrupts the faster it will get through it